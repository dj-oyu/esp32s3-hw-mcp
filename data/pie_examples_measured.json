{
 "provenance": {
  "kind": "measured_on_hardware",
  "note": "Silicon answers, not document text. Every entry here was produced by examples/firmware on a real ESP32-S3, where the same code that ran also compared its result against a scalar C reference written from the TRM pseudo-code, and tools/check_examples_log.py re-derived the values a third time in Python from the log's own inputs. Keep this tier separate from data/pie_instructions.json (PDF-derived) and data/pie_timing_measured.json (stage measurements): the point of the split is that a claim here is reproducible from the log, not quoted from a page.",
  "log": "pie-examples-20260914T183007Z.log",
  "report": "pie-examples-report-20260914T183007Z.json",
  "firmware_git_rev": "05e122d-dirty",
  "device": "M5Stack Cardputer / ESP32-S3 (QFN56, revision v0.2), serial AC:A7:04:01:2E:80",
  "date": "2026-09-14",
  "run_summary": "ex01-ex09: 45 of 45 checks pass (the device firmware agrees with its own C reference and with the host-side Python model). ex07 and ex08 failed in the run before this one and both failures were ours, not the manual's: ex08 mixed signed and unsigned multiply-shift semantics, and ex07/ex09 laid out four-lane coefficient rows inside a loop that advances the address register by 16 bytes per 128-bit load.",
  "runs": [
   "pie-examples-20260914T174138Z.log: 21 ok / 85 fail in the firmware, 17 of 22 host checks -- the two failures were this side's errors (a sliding window off the 16-byte grid, and R2BF sel2 read LSB-first)",
   "pie-examples-20260914T174958Z.log: 24 of 24 host checks pass after fixing both; the hardware side of every claim below now comes from this run",
   "pie-examples-20260914T183007Z.log: 45/45 (ex01-ex09 green, host checker and firmware self-check)"
  ]
 },
 "findings": [
  {
   "id": "post_increment_step_accx_forms",
   "instructions": [
    "EE.LD.ACCX.IP",
    "EE.ST.ACCX.IP",
    "EE.LD.128.USAR.IP",
    "EE.VLD.128.IP"
   ],
   "claim": "The post-increment amount is {20{imm16[7]}, imm16[7:0], 4{0}} for the 128-bit forms and {21{imm8[7]}, imm8[7:0], 3{0}} for the ACCX forms: the printed immediate is added verbatim, i.e. the assembler's scale is the hardware's scale.",
   "method": "Each form ran once in a tiny function that returned address_after - address_before; for the disputed EE.ST.ACCX.IP the instruction was emitted as raw bytes from the manual's own instruction-word diagram with field = 1, a value the assembler refuses.",
   "evidence": {
    "printed_immediate_16": {
     "ld_accx": 16,
     "st_accx": 16,
     "ld128_usar": 16,
     "vld128": 16
    },
    "raw_field_1_bytes": {
     "EE.LD.ACCX.IP": 8,
     "EE.ST.ACCX.IP": 8
    },
    "raw_words": {
     "EE.LD.ACCX.IP a3, field=1": "0x0E0134 (.byte 34 01 0e)",
     "EE.ST.ACCX.IP a3, field=1": "0x020134 (.byte 34 01 02)"
    }
   },
   "status": "confirmed",
   "resolves": "data/pie_encoding_errata.json :: EE.ST.ACCX.IP",
   "resolution": "The assembler is right. The manual's printed syntax range for EE.ST.ACCX.IP (-512..508, a 4-byte step over an 8-bit field) and its printed Operation (`as += imm8`, a 1-byte step) are both defects; the 8-byte step of the neighbouring EE.LD.ACCX.IP pseudo-code is what the silicon does. The errata entry stays as the record of the disagreement; this entry is the answer."
  },
  {
   "id": "srs_accx_shift_and_saturation",
   "instructions": [
    "EE.SRS.ACCX"
   ],
   "claim": "au = saturate_to_int32(ACCX >> as[5:0]), and the shift amount comes from a register operand, not an immediate.",
   "evidence": {
    "ACCX=2^38+12345 shift=0": {
     "got": 2147483647,
     "expected": 2147483647,
     "note": "saturated into 32 bits"
    },
    "ACCX=2^38+12345 shift=8": {
     "got": 1073741872,
     "expected": 1073741872,
     "note": "exact, no saturation"
    },
    "ACCX=-(2^38+999) shift=5": {
     "got": -2147483648,
     "expected": -2147483648,
     "note": "saturated negative"
    }
   },
   "status": "confirmed"
  },
  {
   "id": "vmulas_s16_accx_sums_the_lanes",
   "instructions": [
    "EE.VMULAS.S16.ACCX"
   ],
   "claim": "EE.VMULAS.S16.ACCX qx, qy sums the eight 16x16 lane products into the single 40-bit ACCX (as opposed to EE.VMULAS.S16.QACC, which keeps eight independent lane accumulators), so two of them plus EE.SRS.ACCX are a complete 16-tap dot product.",
   "method": "A 16x16 int16 matrix multiply (two VMULAS per output element, B transposed so both operands are contiguous 128-bit chunks) against a scalar C reference in the firmware and the same reference again in Python.",
   "evidence": {
    "products_matching": "256 of 256",
    "cycles_us_measured": 30
   },
   "status": "confirmed"
  },
  {
   "id": "accx_is_a_saturating_40_bit_accumulator",
   "instructions": [
    "EE.VMULAS.S16.ACCX",
    "EE.SRS.ACCX",
    "EE.ZERO.ACCX"
   ],
   "claim": "ACCX saturates at 2^39-1 / -2^39 rather than wrapping, so an overflowing accumulation silently stops growing instead of producing a wrapped value.",
   "method": "32000x32000 per lane, 8 lanes per instruction, 200 instructions: the mathematical sum is 1.6384e12, well past the 40-bit range, and the accumulator's value was read out twice, once where it saturates (shift 0 -> sat32) and once where it does not (shift 9).",
   "evidence": {
    "mathematical_sum": 1638400000000,
    "accx_clamped": 549755813887,
    "accx_clamped_equals": "2^39-1",
    "readout_shift9_exact": 1073741823,
    "readout_shift0_sat32": 2147483647
   },
   "status": "confirmed"
  },
  {
   "id": "r2bf_operand_lists_are_msb_first",
   "instructions": [
    "EE.FFT.R2BF.S16"
   ],
   "claim": "The manual writes R2BF's operand lists MSB first: for {qy[95:64], qy[31:0], qx[95:64], qx[31:0]} the leftmost 32-bit field occupies the highest lane pair. With qx = qy = eight int16 samples, sel2=0 yields [x0+x4, x1+x5, x2+x6, x3+x7, x0-x4, x1-x5, x2-x6, x3-x7] and sel2=1 yields [x0+x2, x1+x3, x4+x6, x5+x7, x0-x2, x1-x3, x4-x6, x5-x7] in qa0.",
   "method": "One R2BF per call with known 8-lane input, compared against the pseudo-code transcribed twice, once with each field ordering.",
   "evidence": {
    "input_x": [
     100,
     -200,
     300,
     -400,
     500,
     -600,
     700,
     -800
    ],
    "sel2_0_hardware": [
     600,
     -800,
     1000,
     -1200,
     -400,
     400,
     -400,
     400
    ],
    "sel2_1_hardware": [
     400,
     -600,
     1200,
     -1400,
     -200,
     200,
     -200,
     200
    ],
    "lsb_first_reading_of_sel2_1": "predicts [1200, -1400, 400, -600, -200, 200, -200, 200]: refuted, the two halves of qa0 swap"
   },
   "status": "confirmed",
   "note": "This was the missing piece for stacking FFT stages: without it a multi-stage kernel fails as 'does not work' rather than 'stage 2 is wrong'. The MSB-first reading now reproduces the hardware exactly for both sel2 values."
  },
  {
   "id": "cmul_complex_multiply_with_sar_shift",
   "instructions": [
    "EE.CMUL.S16"
   ],
   "claim": "EE.CMUL.S16 qz, qx, qy, sel4 does two complex multiplies on interleaved (re, im) lane pairs (re = ac - bd, im = ad + bc) and shifts the result arithmetically right by SAR[5:0], with sel4 selecting lanes 0..3 or 4..7.",
   "evidence": {
    "sel4_0_sar_0": [
     -3936,
     6784,
     -8416,
     -6784
    ],
    "sel4_1_sar_0": [
     -13152,
     -3392,
     10976,
     -3392
    ],
    "sel4_0_sar_12": [
     415,
     97,
     1293,
     -98
    ],
    "sel4_1_sar_12": [
     -3492,
     -49,
     5346,
     -49
    ]
   },
   "status": "confirmed",
   "note": "SAR was set with the native `ssr` instruction. No saturation was observed at these magnitudes; whether the shift saturates is untested."
  },
  {
   "id": "vld128_drops_the_low_address_bits",
   "instructions": [
    "EE.VLD.128.IP",
    "EE.VST.128.IP"
   ],
   "claim": "A 128-bit PIE access forms its address as {as[31:4], 4{0}}: the low four address bits are dropped, not honoured, so an access off the 16-byte grid silently reads a different 16 bytes (and does not fault).",
   "method": "A 16-tap sliding FIR written the obvious way (EE.VLD.128.IP from x + 2*n) was run against a scalar reference; afterwards the same log was checked against a model that rounds every window down to the 16-byte grid.",
   "evidence": {
    "naive_sliding_fir_mismatches": "42 of 49 outputs",
    "mismatches_exactly_at": "the samples whose window already starts on the grid: n = 0, 8, 16, 24, 32, 40, 48",
    "rounded_window_model_reproduces_the_hardware": "49 of 49 outputs"
   },
   "status": "confirmed",
   "citation": {
    "document": "ESP32-S3 TRM v1.8",
    "page": 49,
    "what": "figure 1.7-1 / the address formation of the PIE memory accesses"
   }
  },
  {
   "id": "ld_qr_interlock_second_harness",
   "instructions": [
    "LD.QR",
    "ST.QR",
    "MV.QR"
   ],
   "claim": "LD.QR's operand interlock costs one cycle when its consumer is the very next instruction and nothing once one instruction separates them -- the same conclusion data/pie_timing_measured.json reached from the other direction (LD.QR def = stage M, consumer reads at stage E).",
   "method": "2000 iterations of [LD.QR q0, buf; 0..2 filler instructions; consumer], once with the consumer reading q0 and once with it reading an unrelated q4, differenced.",
   "evidence": {
    "distance_0": {
     "cycles_per_iteration": 1.0,
     "dep": 15999,
     "ind": 13999
    },
    "distance_1": {
     "cycles_per_iteration": 0.0,
     "dep": 15999,
     "ind": 15999
    },
    "distance_2": {
     "cycles_per_iteration": 0.042,
     "note": "within the noise of this harness"
    }
   },
   "status": "confirmed",
   "note": "Also confirmed on this run: LD.QR/ST.QR copy 256 bytes correctly and MV.QR moves a 128-bit register, so the three instructions Table 1.7-2 omits are usable as documented."
  },
  {
   "id": "src_q_operand_order_and_the_funnel_path",
   "instructions": [
    "EE.SRC.Q",
    "EE.LD.128.USAR.IP"
   ],
   "claim": "EE.SRC.Q qz, qa, qb computes qz = {qb, qa} >> (SAR_BYTE*8): the FIRST operand is qs0 (the low 128 bits) and the second is qs1 (the high half). Combined with EE.LD.128.USAR.IP -- which loads the aligned chunk and parks the dropped address bits in SAR_BYTE -- this reads a 16-byte window that starts off the 16-byte grid, with no staging copy.",
   "method": "SRC.Q ran twice in one call, once with (q0=aligned chunk, q1=next chunk) and once with the operands swapped, from a pointer 2 bytes off the grid; the two outputs differ and one of them equals the window starting at byte offset 2 (x[1..8]).",
   "evidence": {
    "window_at_byte_2": [
     628,
     -759,
     1924,
     -452,
     1299,
     -2815,
     -2611,
     2184
    ],
    "EE.SRC.Q q3, q0, q1": [
     628,
     -759,
     1924,
     -452,
     1299,
     -2815,
     -2611,
     2184
    ],
    "EE.SRC.Q q4, q1, q0": [
     755,
     2663,
     -993,
     1250,
     24,
     -1674,
     320,
     2745
    ],
    "verdict": "ab=1 ba=0, so the first operand is qs0"
   },
   "status": "confirmed",
   "note": "Also confirmed by the same probe: EE.LD.128.USAR.IP loads the aligned chunk (its q0 equals the bytes at x) while recording SAR_BYTE = 2."
  },
  {
   "id": "qacc_accumulate_to_readout_needs_no_gap",
   "instructions": [
    "EE.VSMULAS.S16.QACC",
    "EE.SRCMB.S16.QACC"
   ],
   "claim": "The accumulator write of a MAC is visible to the readout at issue distance 0: one VMULAS followed immediately by SRCMB returns the product, and four VMULAS followed immediately by SRCMB return the four-term sum. Table 1.7-2 lists no accumulator def, so the static model predicts 0 cycles and the hardware agrees -- in this pattern, at 240 MHz, with the loop in IRAM.",
   "method": "ex09: one MAC with the readout 0/1/2/3/4/6 slots later (identical results at every distance), and ex07's four-MAC row at 0, 2 and 4 slots.",
   "evidence": {
    "mac1_min_gap": 0,
    "mac4_gap0_matches_model": 1,
    "mac4_gap2_matches_model": 1,
    "mac4_gap4_matches_model": 1,
    "chain_32_of_32_rows_at_gap0": true
   }
  },
  {
   "id": "srcmb_s16_qacc_writes_the_accumulator_back",
   "instructions": [
    "EE.SRCMB.S16.QACC"
   ],
   "claim": "SRCMB.S16.QACC is a read-modify-write: the shifted, saturated values it produces are written back into the 40-bit lanes. A first readout with shift 5 followed by a second with shift 0 returns the first readout's values, not the original accumulator.",
   "method": "ex09_srcmb_wb: one MAC, then SRCMB twice with different shifts, into two buffers.",
   "evidence": {
    "verdict": "read_modify_write",
    "wb_a": [
     93,
     -188,
     281,
     -375,
     468,
     -563,
     656,
     -750
    ],
    "wb_b": "identical to wb_a"
   }
  },
  {
   "id": "qacc_raw_lane_layout",
   "instructions": [
    "RUR.QACC_L_0..4",
    "RUR.QACC_H_0..4",
    "EE.VSMULAS.S16.QACC"
   ],
   "claim": "For the 16-bit MAC the eight 40-bit accumulator lanes are laid out as QACC_L bits 0..159 = lanes 0..3 and QACC_H bits 0..159 = lanes 4..7, 40 bits per lane, so a lane read through RUR is (QACC_H_i[7:0] << 32) | QACC_L_i. QACC_L_4/QACC_H_4 hold the top 32 bits (only 8 of them used).",
   "method": "ex09_raw_qacc: one MAC (v = 1000, -2000, ... with coefficient lane 0 = 3), then the ten RUR words.",
   "evidence": {
    "lanes": [
     3000,
     -6000,
     9000,
     -12000,
     15000,
     -18000,
     21000,
     -24000
    ],
    "expected": "v[j] * 3, all eight lanes exact"
   }
  },
  {
   "id": "zero_qacc_is_ordered_before_the_next_mac",
   "instructions": [
    "EE.ZERO.QACC",
    "EE.VSMULAS.S16.QACC"
   ],
   "claim": "EE.ZERO.QACC placed between two MACs is ordered: the second MAC's readout holds only its own product, not the sum of both.",
   "method": "ex09_zero_vis: MAC with the sel8 = 0 coefficient, ZERO.QACC, MAC with sel8 = 1, readout.",
   "evidence": {
    "readout": "v * coef[1]",
    "would_be_unordered": "v * (coef[0] + coef[1])"
   }
  },
  {
   "id": "sel8_broadcasts_the_named_lane",
   "instructions": [
    "EE.VSMULAS.S16.QACC"
   ],
   "claim": "sel8 selects the 16-bit lane of the coefficient register that is broadcast against all eight lanes of the source register (verified for sel8 = 1, 2, 3, not just 0).",
   "method": "ex09_mac1_s1/s2/s3_g4 with distinct coefficient lanes.",
   "evidence": {
    "sel1": "v * 7",
    "sel2": "v * 5",
    "sel3": "v * 11"
   }
  },
  {
   "id": "128bit_post_increment_is_16_bytes_so_rows_must_be_padded",
   "instructions": [
    "EE.VLD.128.IP",
    "EE.VST.128.IP",
    "EE.VSMULAS.S16.QACC"
   ],
   "claim": "A 128-bit post-increment form always advances the address register by 16 bytes (the field is scaled by 16), so a per-row array whose row is shorter than 16 bytes -- a 4x4 int16 matrix row is 8 bytes -- makes a row loop read every OTHER row and then walk off the end of the array. Nothing faults, the answers are simply from the wrong rows. Rows must be padded to eight int16 lanes.",
   "method": "ex09: the looped row sequence with 8-byte rows came back with row 1 = the answer for coefficient row 2, row 2 = row 0's answer and row 3 = data from the array that happens to follow (the .rodata layout is in the log's build); with the rows padded to 16 bytes the same loop is 32/32, and the .IP walk reproduces the padded array exactly.",
   "evidence": {
    "padded_rows_chain": "32/32",
    "ipwalk_matched": "32/32",
    "unpadded_symptom": "row1 = f(coef row 2), row2 = f(coef row 0), row3 = f(adjacent array)"
   }
  },
  {
   "id": "pie_buffers_must_be_declared_aligned_16",
   "instructions": [
    "EE.VLD.128.IP",
    "EE.ANY.128BIT.ACCESS"
   ],
   "claim": "A C array handed to a 128-bit PIE access must be 16-byte aligned: the hardware forces the low four address bits to zero rather than faulting, so a misaligned array is read from the boundary below it and every lane comes back shifted by (address & 15) / 2 elements. `static const int16_t` arrays are only 2-byte aligned and landed 4 bytes off the grid in this build.",
   "method": "Two device runs: the first ex07/ex09 run returned values two lanes to the right of the model, and the ELF symbols confirmed the arrays sat at address mod 16 == 4. The examples now declare every kernel buffer __attribute__((aligned(16))) and assert it on the device (check_aligned).",
   "evidence": {
    "misaligned_symbols_mod16": {
     "coef8": 4,
     "v8": 4,
     "coef_rows": 4,
     "v_rows": 4,
     "verts": 4
    },
    "after_fix": "all alignment checks pass and the values match the models"
   }
  },
  {
   "id": "vmul_s16_is_arithmetic_and_vmul_u16_is_logical",
   "instructions": [
    "EE.VMUL.S16",
    "EE.VMUL.U16"
   ],
   "claim": "With a register of ones and SAR = 1, EE.VMUL.S16 shifts the SIGNED lane right arithmetically (a lane with bit 15 set comes back sign-extended, 0x8AD0 -> 0xC168) while EE.VMUL.U16 shifts the unsigned lane logically (0x4568). The set has no shift-right instruction, so this pair is how a per-lane shift is spelled -- and picking the wrong one is silently wrong for packed unsigned data such as RGB565.",
   "method": "ex08_shift_sign: the same lanes shifted by both instructions in one call; ex08's half blend uses U16.",
   "evidence": {
    "shift_in_0": 30000,
    "signed_0": 15000,
    "unsigned_0": 15000,
    "shift_in_1": -30000,
    "signed_1": -15000,
    "unsigned_1": 17768
   }
  },
  {
   "id": "vmul_truncates_it_does_not_saturate",
   "instructions": [
    "EE.VMUL.S16",
    "EE.VMUL.U16"
   ],
   "claim": "A vector multiply keeps the low 16 bits of the shifted product: an oversized tint wraps instead of clamping. Saturating behaviour needs an explicit VMIN/VMAX or VADDS pass.",
   "method": "ex08_tint over 1024 pixels with tint = 300 (Q8): the C reference reproduces the wrap and matches.",
   "evidence": {
    "pixels_matching": "1024/1024",
    "reference": "(int16_t)((a[i] * 300) >> 8)"
   }
  }
 ],
 "printed_not_interpreted": [
  {
   "id": "bitrev_operand",
   "instruction": "EE.BITREV",
   "what": "Four words for two different operand values, raw, because the expected value cannot be constructed: the result is gated by the special register FFT_BIT_WIDTH, and no instruction that sets it appears in the PIE instruction list.",
   "evidence": {
    "operand_0x3fc94040": [
     "0x00820040",
     "0x00c20042",
     "0x00a20044",
     "0x00e20062"
    ],
    "operand_0x3fc94050": [
     "0x008a0050",
     "0x00ca0052",
     "0x00aa0054",
     "0x00ea006a"
    ]
   },
   "note": "The two calls differ only in the operand (16 bytes apart) and their results differ, so the operand does influence the result; which reading (address of the data, or the value itself) is still open. TRM 1.8 lists the operand as `as`, Table 1.7-2 as `ax`."
  }
 ],
 "open_after_this_run": [
  "A multi-stage real FFT: sel2=0 and sel2=1 are pinned down, so the stage-to-stage lane layout can be written and checked stage by stage.",
  "EE.SRCMB.S8.QACC / the eight-parallel QACC readout path for 8-bit data (S16 is now settled, S8 is not).",
  "EE.LDQA/EE.STQA (the QACC memory forms) and EE.SRCQ.128.ST.INCP: the accumulate-into-memory traffic has not been measured.",
  "EE.VZIP/EE.VUNZIP lane order and EE.MOVI.32.A/Q sel4 mapping: esp-dl's skill lists the mnemonics but no lane semantics.",
  "EE.BITREV with FFT_BIT_WIDTH (the register exists per esp-dl's skill; the examples left it uninterpreted).",
  "Whether a PIE kernel's context survives a FreeRTOS task switch (COP3 save area) on this chip."
 ]
}